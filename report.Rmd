---
title: "Estimated Farm Mediated Nitrogen Removal"
output: pdf_document
date: "`r format(Sys.Date(),'%B %e, %Y')`"
params: 
  n: NA
  table: 'NULL'
  plot: 'NULL'
  Location: NA
  Tissue.N: NA
  Shell.N: NA
  Tissue.P: NA 
  Shell.P: NA 
  Total.N: NA  
  Total.P: NA
  Units: NA 
  gear: NA 
  ploidy: NA  
  hsize: NA  
  Num: NA
  htime: NA 
  loc2: NA
  Lat: NA
  Lon: NA
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = F)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r, echo=FALSE}
# barplot(c(params$Tissue.N, params$Shell.N), col = 'lightblue', border = 'white', xlab="N removal)", names.arg=c("Tissue N", "Shell N"), ylab=params$Units)
params[["plot"]]
```

```{r, echo=FALSE}
      # kable(
      # data.frame("Tissue N"=params$Tissue.N,"Shell N"=params$Shell.N,
      #             "Total N"=params$Total.N, "Units"=params$Units)
      # )
params[["table"]]
```

```{r, results='asis',echo=FALSE,warning=FALSE}
print(params[["Location"]])
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
